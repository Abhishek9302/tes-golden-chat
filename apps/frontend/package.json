{
  "name": "tes-golden-chat-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "NODE_ENV=production next build && S=$(dirname $(find .next/standalone -maxdepth 4 -name server.js | head -1)) && (test -d public && cp -r public \"$S/\" || true) && mkdir -p \"$S/.next\" && cp -r .next/static \"$S/.next/\"",
    "start": "NODE_ENV=production node $(find .next/standalone -maxdepth 4 -name server.js | head -1)"
  },
  "dependencies": {
    "next": "^14.2.35",
    "react": "^18",
    "react-dom": "^18",
    "tailwindcss": "^3.4",
    "postcss": "^8.4",
    "autoprefixer": "^10.4",
    "typescript": "^5.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18"
  }
}
